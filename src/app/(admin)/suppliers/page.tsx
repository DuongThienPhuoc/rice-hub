import SupplierTable from '@/app/(admin)/suppliers/table';

export default function SupplierPage() {
    return (
        <section className="container mx-auto">
            <SupplierTable />
        </section>
    );
}
